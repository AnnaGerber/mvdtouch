float x1, x2, w, y1, y2;

do {
	x1 = 2.0 * ranf() - 1.0;
	x2 = 2.0 * ranf() - 1.0;
	w = x1 * x1 + x2 * x2;
} while ( w >= 1.0 );

w = sqrt( (-2.0 * ln( w ) ) / w );
// generate normal distribution random values
y1 = x1 * w;
y2 = x2 * w;

